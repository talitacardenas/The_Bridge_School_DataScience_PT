import functions as F
import logging
if __name__ == '__main__':
  F.create_logfile()
  F.setArguments(F.concatena_args(opType,c_env))
  opType = F.options.opType
  c_env = F.options.env
  F.concatena_args(opType, c_env)